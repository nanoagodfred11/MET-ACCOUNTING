# MongoDB connection string
MONGODB_URI=mongodb://localhost:27017/met_accounting

# Authentication secrets (REQUIRED in production)
JWT_SECRET=
SESSION_SECRET=
ACCESS_PASSWORD=

# Optional: JWT expiry in seconds (default: 604800 = 7 days)
# JWT_EXPIRES_IN=604800
